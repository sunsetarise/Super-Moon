# SM35 GPU qualification report

The CUDA driver requires CUDA tooling, CuPy, an explicit execution authorization,
real device properties, device arrays, synchronized CUDA events, CPU parity, and
separate transfer/kernel timing. It has no CPU fallback qualification path.

No CUDA device or CUDA tooling is visible here. The GPU receipt is UNAVAILABLE,
the reviewer decision is BLOCKED, and no speedup or parity claim is made.
