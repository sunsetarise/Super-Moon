# SM36 architecture and qualification status

SM36 is append-only: the supplied SM35 gzip is the exact byte prefix and a new
deterministic gzip member carries SM36 frames. Its registry compiles every one of
the 15,000 prompt methodologies into a validated record and executable binding.
Each phase has 50 technique bindings crossed with 50 evidence lenses; the engine
requires technique evidence, lens evidence, provenance, preregistered gates, and
independent review. Physical phases additionally require explicit authorization
and physical-execution attestation.

The twelve weighted gates are non-bypassable. Coverage, architecture, simulated
receipts, or averages cannot close a missing physical gate. Local work can verify
source preservation, registry completeness, contracts, unit/regression behavior,
fuzz/mutation resistance, security rules, packaging, and evidence integrity. The
release stays `BLOCKED_PENDING_REAL_EXECUTION` until PETSc/MPI multi-node, Slurm,
OpenFOAM, SU2, OCCT CAD, CUDA, 24/72-hour endurance, industrial controls,
certification-readiness objectives, and independent reproduction are supported by
authentic independently reviewed receipts.
