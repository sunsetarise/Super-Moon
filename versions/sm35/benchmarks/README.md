# Benchmark protocol

Correctness gates precede performance. Every external benchmark records fixed
work units, warm-up, at least three raw samples, median, MAD, confidence interval,
coefficient of variation, hardware/runtime identity, affinity, and equal output
fidelity. Speedup, strong/weak efficiency, Karp-Flatt fraction, and cost are not
reported until the corresponding physical backend runs.
